import * as React from 'react';


const currencyRow = () => {
  return(
  <div>
<input type="numbe" />
  <select className="input">
    <option value="HI">HI</option>
    </select>
</div>
  )}

export default currencyRow 